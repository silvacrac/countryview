specVersion: "v1beta"
serviceId: "countryview"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "countryview-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
